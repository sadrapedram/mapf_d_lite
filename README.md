# Traffic Simulation (OSMnx)
github address: https://github.com/sadrapedram/mapf_d_lite.git
```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python graph_v5.py        # run traffic sim
python astar_vs_dfs.py    # A* vs DFS demo
python main_astar_vs_dfs.py  # batch stats -> CSV
```
